spring:
  datasource:
    url: jdbc:mysql://localhost:3306/guesstest?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 8080

cucumber:
  port: 8080
  context-path:
